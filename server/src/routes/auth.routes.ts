/**
 * auth.routes.ts — Authentication route stubs.
 *
 * Endpoints (to be implemented in task #005):
 *   POST /api/v1/auth/login    — authenticate with username + password (FR-001)
 *   POST /api/v1/auth/logout   — invalidate the current session (FR-001)
 *   PATCH /api/v1/auth/password — change the authenticated user's password (FR-005)
 */

import { Router } from 'express';

const router = Router();

// TODO (task #005): implement POST /auth/login
// TODO (task #005): implement POST /auth/logout
// TODO (task #005): implement PATCH /auth/password

export default router;
