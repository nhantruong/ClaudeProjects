/**
 * dashboard.routes.ts — Dashboard route stubs.
 *
 * Endpoints (to be implemented in task #010):
 *   GET /api/v1/dashboard — dashboard summary: active projects, task counts
 *                           (due today, overdue, completed this week),
 *                           team workload, PPC trend (FR-070 – FR-073)
 */

import { Router } from 'express';

const router = Router();

// TODO (task #010): implement GET /dashboard

export default router;
