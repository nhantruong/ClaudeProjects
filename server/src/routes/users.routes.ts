/**
 * users.routes.ts — User management route stubs.
 *
 * Endpoints (to be implemented in task #007):
 *   GET  /api/v1/users/me    — return authenticated user's profile
 *   GET  /api/v1/users        — list all users (admin only, FR-010)
 *   POST /api/v1/users        — create a user account (admin only, FR-003, FR-010)
 *   PATCH /api/v1/users/:id   — update a user account (admin only, FR-010)
 *   DELETE /api/v1/users/:id  — deactivate a user account (admin only, FR-010)
 */

import { Router } from 'express';

const router = Router();

// TODO (task #007): implement GET /users/me
// TODO (task #007): implement GET /users
// TODO (task #007): implement POST /users
// TODO (task #007): implement PATCH /users/:id
// TODO (task #007): implement DELETE /users/:id

export default router;
